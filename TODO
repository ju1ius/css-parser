PHPDOC !!!
TEST SUITE !!!
README


Unicode escapes:
  @\\0070 age{} should turn into a @page rule

CSSOM:
  DOMDocument extensions
  Selectors:
    - Tests
  shorthands:
    expand:
      expand if more than one background shorthand ?
      liststyle tests
    create:
      liststyle tests
      background-size syntax in background shorthand ?
  setCssText() methods
  StyleSheetList (http://dev.w3.org/csswg/cssom/#the-stylesheetlist-sequence):
    resolving imports
  URL:
    absolutize urls

Parser:
  Charset handling
  background-size syntax in background shorthand
  border-radius
  The parser code is starting to be a bit hairy...
  Should I use a parser generator ?
    + maintainability
    - performance ?
